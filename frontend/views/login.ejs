<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'BuildYourPC - Login' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/login.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="h-screen font-['Inter'] bg-navy-900 text-blue-100">
    <div class="flex h-screen flex-col lg:flex-row">
        <!-- Left Side (Image and Text) -->
        <div class="relative flex-1 hidden lg:flex items-center justify-center overflow-hidden">
            <img src="https://images.unsplash.com/photo-1628269989095-ef8569497706?q=80&w=1528&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" 
                 alt="Gaming PC Setup" 
                 class="absolute inset-0 w-full h-full object-cover">
            <div class="absolute inset-0 bg-gradient-to-br from-blue-600/40 to-navy-900/90"></div>
            <div class="relative z-10 text-center p-6">
                <h2 class="text-4xl font-bold text-white">Build Your PC</h2>
                <p class="text-blue-100 text-lg max-w-md mx-auto mt-4">Customize your PC effortlessly.</p>
            </div>
        </div>

        <!-- Right Side (Login Form) -->
        <div class="flex w-full lg:w-1/2 items-center justify-center p-6 bg-navy-900">
            <div class="w-full max-w-md space-y-8">
                <!-- Title -->
                <div class="text-center">
                    <div class="flex justify-center mb-4 animate-float">
                        <div class="relative">
                          <div class="absolute inset-0 bg-blue-500 rounded-xl blur-xl opacity-30"></div>
                          <div class="relative bg-navy-800 p-4 rounded-xl border border-blue-500/30">
                            <svg class="h-12 w-12 text-blue-400" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                              <rect width="20" height="14" x="2" y="3" rx="2"/>
                              <line x1="2" x2="22" y1="20" y2="20"/>
                            </svg>
                          </div>
                        </div>
                      </div>
                    <h1 class="text-3xl font-bold text-white">Welcome to BuildYourPC</h1>
                    <p class="text-blue-100 text-base mt-2">Sign in to start building your perfect PC</p>
                </div>

                <!-- Login Form -->
                <div class="bg-navy-800/50 backdrop-blur-xl rounded-2xl shadow-lg p-6 border border-white/10">
                    <form id="loginForm" class="space-y-4" action="<%= action || '/login' %>" method="POST">
                        <div class="space-y-2">
                            <label for="email" class="text-base font-medium block">Email</label>
                            <input type="email" id="email" name="email" 
                                   class="block w-full px-4 py-3 bg-navy-900/50 border border-blue-500/30 rounded-lg text-base text-black"
                                   placeholder="Enter your email" value="<%= email || '' %>">
                        </div>
                        <div class="space-y-2">
                            <label for="password" class="text-base font-medium block">Password</label>
                            <input type="password" id="password" name="password" 
                                   class="block w-full px-4 py-3 bg-navy-900/50 border border-blue-500/30 rounded-lg text-base text-black"
                                   placeholder="Enter your password">
                        </div>
                        <div class="flex items-center justify-between text-sm">
                            <div class="flex items-center">
                                <input type="checkbox" id="remember" name="remember" class="mr-2">
                                <label for="remember">Remember me</label>
                            </div>
                            <a href="#" class="text-blue-300 hover:underline">Forgot password?</a>
                        </div>
                        <button type="submit" 
                                class="w-full py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg text-base">
                            Sign In
                        </button>
                    </form>
                    <p class="mt-6 text-center text-sm">
                        Don't have an account? 
                        <a href="<%= signupUrl || '#' %>" class="text-blue-300 hover:underline font-medium">Sign up</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
