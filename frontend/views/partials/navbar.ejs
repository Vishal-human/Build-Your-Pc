<nav class="bg-slate-900 text-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
            <!-- Logo/Brand -->
            <div class="flex-shrink-0 flex items-center">
                <a href="/"
                    class="text-xl font-bold text-blue-400 hover:text-blue-300 transition duration-300 flex items-center gap-2">

                    BuildYourPC
                </a>
            </div>

            <!-- Navigation Links -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="/" class="hover:text-blue-400 transition duration-300 flex items-center gap-1">
                    Home
                </a>
                <a href="/componentselection"
                    class="hover:text-blue-400 transition duration-300 flex items-center gap-1">
                    Build PC
                </a>

                <a href="/about" class="hover:text-blue-400 transition duration-300 flex items-center gap-1">
                    About
                </a>

                <% if (session && session.user) { %>
                    <!-- User Greeting -->
                    <a href="/orders" class="hover:text-blue-400 transition duration-300 flex items-center gap-1">
                        Orders
                    </a>
                    <span class="text-gray-300 flex items-center gap-2">
                        Hello, <strong class="text-white">
                            <%= session.user.fullname %>
                        </strong>
                    </span>

                    <!-- Logout Button -->
                    <button onclick="event.preventDefault(); document.getElementById('logout-form').submit();"
                        class="bg-red-600 px-4 py-2 rounded-lg hover:bg-red-700 transition duration-300 flex items-center gap-1">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                        </svg>
                        Logout
                    </button>
                    <form id="logout-form" action="/logout" method="POST" class="hidden"></form>
                    <% } else { %>
                        <a href="/login" class="hover:text-blue-400 transition duration-300 flex items-center gap-1">
                            Login
                        </a>
                        <a href="/signup"
                            class="bg-blue-600 px-4 py-2 rounded-lg hover:bg-blue-700 transition duration-300 flex items-center gap-1">
                            Sign Up
                        </a>
                        <% } %>
            </div>

            <!-- Mobile Menu Button -->
            <div class="md:hidden">
                <button id="mobile-menu-button"
                    class="text-gray-300 hover:text-white p-2 rounded-lg hover:bg-slate-800 transition duration-300">
                    <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden md:hidden pb-4 space-y-2">
            <a href="/" class="block px-4 py-2 rounded-lg hover:bg-slate-800 transition duration-300">Home</a>
            <a href="/cpuselect" class="block px-4 py-2 rounded-lg hover:bg-slate-800 transition duration-300">Build
                PC</a>
            <a href="/about" class="block px-4 py-2 rounded-lg hover:bg-slate-800 transition duration-300">About</a>

            <% if (session && session.user) { %>
                <!-- Mobile User Greeting -->
                <span class="block px-4 py-2 text-gray-300">Hello, <%= session.user.fullname %></span>

                <!-- Mobile Logout Button -->
                <a href="#"
                    class="block px-4 py-2 bg-red-600 rounded-lg hover:bg-red-700 transition duration-300 text-center"
                    onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
                    Logout
                </a>
                <form id="logout-form" action="/logout" method="POST" class="hidden"></form>
                <% } else { %>
                    <a href="/login"
                        class="block px-4 py-2 rounded-lg hover:bg-slate-800 transition duration-300 text-center">Login</a>
                    <a href="/signup"
                        class="block px-4 py-2 bg-blue-600 rounded-lg hover:bg-blue-700 transition duration-300 text-center">Sign
                        Up</a>
                    <% } %>
        </div>
    </div>
</nav>

<script>
    document.getElementById('mobile-menu-button').addEventListener('click', () => {
        const mobileMenu = document.getElementById('mobile-menu');
        mobileMenu.classList.toggle('hidden');
    });
</script>